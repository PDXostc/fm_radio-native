
The wkb-client is a XWalk plugin to configure the Tizen Weekeyboard. 

To build use:

	> gbs build -A i586

### Weekeyboard #############################################################

The weekeyboard native tizen keyboard application runs as a daemon. It is
is available at:

	git clone ssh://<id>@review.tizen.org:29418/profile/ivi/weekeyboard

   Use the Tizen branch with:

	git checkout -b tizen origin/tizen
	git reset --hard origin/tizen

