
extension_common is a static library that must be linked into all
extensions.

To build, use:

	> gbs build -A i586

The result will be a static library libextension-common.a in the 
directory ../lib


